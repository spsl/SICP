#lang rackET

(define (append x y)
  (if (null? x)
      y
      (cons (car x)
            (append (cdr x) y))))

(define (accumulate op initial sequence)
  (if (null? sequence)
      initial
      (op (car sequence)
          (accumulate op initial (cdr sequence)))))

