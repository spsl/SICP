#lang racket



(define (for-each proc items)
  (define (iter things)
    (cond ((not (null? things))
           (begin
             (proc (car things))
             (iter (cdr things))))))
  (iter items))